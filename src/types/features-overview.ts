export interface FeaturesOverviewDoc {
  // Frontmatter fields
  title: string;
  description: string;
  seoTitle?: string;
  seoDescription?: string;
  lastModified?: string;
  
  // Content
  content: string;
}
